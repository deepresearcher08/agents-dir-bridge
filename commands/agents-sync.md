---
description: Re-run the .agents/ directory bridge sync without restarting the session
---

Run this exact command and report its output to the user verbatim, then briefly summarize what changed (or say nothing changed if the output was empty):

```
node "$CLAUDE_PLUGIN_ROOT/scripts/sync-agents-dir.js" <<< '{}'
```
